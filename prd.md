# 产品需求文档 (PRD) - PromptSnap v0.1 (MVP)

| 项目 | 内容 |
| :--- | :--- |
| **产品名称** | PromptSnap |
| **产品类型** | Chrome 浏览器扩展 (Extension) |
| **核心交互** | 浏览器扩展弹窗 (Popup) + 页面内自动填入 |
| **当前阶段** | MVP (最小可行性产品) |
| **目标平台** | 任意 AI Web Chat 网站，优先兼容常见对话式输入页面 |
| **核心目标** | 在 AI 对话页面中一键填入和复用预设 Prompt，解决重复输入与跨站复用痛点。 |

---

## 1. 产品背景与价值
**痛点**：用户在与各类 AI Web Chat 产品交互时，高频使用固定指令（如“请解释代码”、“翻译成英文”、“润色文案”）。手动输入或从记事本复制粘贴效率低下，而且常常需要跨站重复操作。
**价值**：在浏览器当前页面通过扩展弹窗“即点即用”，并支持跨站执行，显著提升 AI 工作流效率。

---

## 2. 用户流程 (User Flow)
1.  用户打开任意 AI Web Chat 网站。
2.  用户点击浏览器工具栏中的 PromptSnap 图标。
3.  扩展弹窗展示 Prompt 列表、分类、复制和执行操作。
4.  用户点击某条 Prompt，或先填写变量后再确认执行。
5.  用户选择在当前页面填入，或选择目标网站并在新标签页中打开执行。
6.  **核心动作**：目标页面的输入框自动填入文字，并触发输入事件。

---

## 3. 功能需求详情 (Functional Requirements)

### 3.1. 唤起与交互 (Toggle & UI)
*   **扩展弹窗 (Popup)**：
    *   点击浏览器扩展图标 -> 打开 PromptSnap 弹窗。
    *   弹窗展示 Prompt 列表、分类标签、设置入口和新增入口。
    *   在需要变量输入时，弹窗内切换到执行配置视图。
*   **执行方式**：
    *   支持在当前活动标签页直接填入。
    *   支持选择已配置站点，在新标签页打开后自动填入。

### 3.2. Prompt 列表管理 (CRUD)
*   **列表展示**：
    *   **标题**：加粗显示。
    *   **预览**：单行截断，显示前 20 字。
    *   **操作**：每行包含 [填入]、[编辑]、[删除] 按钮。
*   **新增/编辑 Prompt**：
    *   点击“+ 新建”或“编辑图标”，进入表单模式。
    *   **字段**：标题 (Title)、分类 (Category)、内容 (Content, 多行文本域)。
    *   **保存**：校验非空后保存至 `chrome.storage.local`。
*   **变量模板**：
    *   支持 `{{变量名}}` 占位符。
    *   支持 `{{变量名:默认值}}` 作为带默认值输入框。
    *   默认值中使用分号分隔多个选项时，渲染为下拉选择框。
*   **数据管理**：
    *   支持导出 Prompt 为 JSON 备份文件。
    *   支持从 JSON 文件导入并合并/覆盖已有 Prompt。
*   **站点管理**：
    *   支持维护常用 AI 网站列表。
    *   执行时可选择“当前页面”或指定目标站点。

### 3.3. 核心功能：智能填入 (Smart Snap)
*   **目标元素定位**：
    *   优先使用当前已聚焦的输入元素。
    *   通用候选类型包括 `textarea`、`input[type="text"]` 与 `div[contenteditable="true"]`。
    *   若页面存在多个候选输入框，优先选择可见且面积较大的主输入区域。
*   **模拟输入 (Critical)**：
    *   不仅是修改 DOM 的 `innerText`。
    *   **必须触发 Input 事件**：执行 `input`, `change`, 或 `textInput` 事件分发，确保目标 AI Web 应用的前端框架（Angular/Lit/React 等）能够感知数据变化，从而激活“发送”按钮。
    *   **光标处理**：填入后，将光标移动到文本末尾，方便用户继续补充内容。
*   **跨站执行**：
    *   用户可在弹窗中选择一个预配置站点。
    *   扩展打开新标签页并在页面加载完成后尝试自动填入。
    *   需要具备重试机制以应对 SPA 页面和延迟加载场景。

---

## 4. 技术与非功能需求

| 需求项 | 说明 |
| :--- | :--- |
| **Manifest版本** | Manifest V3 |
| **权限控制** | `storage` (存储数据) |
| **Host Permissions** | 允许在目标 AI Web Chat 页面注入 content script |
| **数据隐私** | 所有 Prompt 数据仅存储在用户本地浏览器 (`local storage`)，不上传云端。 |

---

## 5. 界面原型示意

```text
+-------------------------------------------------------+
| 网页主体 (任意 AI Web Chat 页面)                      |
|                                                       |
|  浏览器工具栏 -> [PromptSnap]                         |
|                                                       |
|   对话区域...                      +-----------------+ |
|                                    | PromptSnap      | |
|                                    +-----------------+ |
|                                    | 分类标签        | |
|                                    +-----------------+ |
|                                    | 代码解释   [>]  | |
|                                    | 英文润色   [>]  | |
|                                    | 总结助手   [>]  | |
|                                    +-----------------+ |
|                                                       |
| 输入框: [ 这里的字是自动填进去的 ]                      |
+-------------------------------------------------------+
```
